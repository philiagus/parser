version: '2'

services:
  test7.4:
    build:
      context: .
      dockerfile: docker/7.4.Dockerfile
    volumes:
      - ./:/app

  test8.0:
    build:
      context: .
      dockerfile: docker/8.0.Dockerfile
    volumes:
      - ./:/app
